# Project Vanguard - Backend

This directory is reserved for future backend services (e.g., Python AI service, separate Node.js API).
Currently, the core application logic and API routes reside in the `frontend` directory using Next.js App Router.
